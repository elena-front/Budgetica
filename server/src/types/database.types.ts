export type CreateUserDTO = {
  name: string;
  email: string;
  password: string;
};

export type SingupData = {
  name: string;
  email: string;
  password: string;
};

export type SignInData = {
  email: string;
  password: string;
};
